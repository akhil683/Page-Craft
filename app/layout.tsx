import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Page Craft",
  description: "Welcome to PageCraft, the ultimate platform for aspiring authors, creators, and storytellers. With our cutting-edge AI tools, you can effortlessly write, design, and publish stunning books—from crafting compelling content to designing eye-catching covers. Whether you're writing a novel, a children's book, or a professional guide, PageCraft empowers you to bring your ideas to life.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
